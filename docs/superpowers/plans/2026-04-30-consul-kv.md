# Consul KV 服務整合 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 docker-compose-php 本地開發環境中加入 Consul KV 服務，支援集中式設定管理，並提供初始 KV 資料匯入功能。

**Architecture:** 使用 `hashicorp/consul:1.19` 單節點 server 模式，開啟 Web UI 與 HTTP API（port 8500），資料持久化至本機 `./consul/data/`。PHP 應用程式透過 `http://consul:8500/v1/kv/<KEY>?raw` 讀取設定值。初始 KV 資料由 `consul/init.sh` 在 Consul 啟動後匯入。

**Tech Stack:** Docker Compose, hashicorp/consul:1.19, Bash, Python3

---

## 檔案清單

| 動作 | 路徑 | 說明 |
|------|------|------|
| 新增 | `consul/config/consul.json` | Consul server 基本設定 |
| 新增 | `consul/data/.gitkeep` | 持久化資料目錄佔位符 |
| 新增 | `consul/kv-init.json` | 初始 KV 資料（大寫蛇式 key） |
| 新增 | `consul/init.sh` | KV 匯入腳本（從宿主機執行） |
| 修改 | `docker-compose.yml` | 新增 consul service |
| 修改 | `.gitignore` | 排除 `consul/data/` 內容 |

---

### Task 1: 建立 consul 目錄與 Consul 設定檔

**Files:**
- Create: `consul/config/consul.json`
- Create: `consul/data/.gitkeep`
- Modify: `.gitignore`

- [ ] **Step 1: 建立目錄結構**

```bash
mkdir -p consul/config consul/data
touch consul/data/.gitkeep
```

- [ ] **Step 2: 建立 `consul/config/consul.json`**

```json
{
  "datacenter": "local",
  "log_level": "INFO",
  "server": true
}
```

- [ ] **Step 3: 在 `.gitignore` 新增排除規則**

在 `.gitignore` 末尾加入：

```
/consul/data/*
!/consul/data/.gitkeep
```

- [ ] **Step 4: 驗證目錄結構**

```bash
find consul/ -type f
```

Expected output:
```
consul/config/consul.json
consul/data/.gitkeep
```

- [ ] **Step 5: Commit**

```bash
git add consul/config/consul.json consul/data/.gitkeep .gitignore
git commit -m "chore: add consul directory structure and server config"
```

---

### Task 2: 新增 Consul 服務至 docker-compose.yml

**Files:**
- Modify: `docker-compose.yml`

- [ ] **Step 1: 在 `docker-compose.yml` 的 `services:` 區塊（redis 服務之後）新增以下內容**

```yaml
    # Consul KV service
    consul:
        image: hashicorp/consul:1.19
        container_name: consul
        ports:
            - "8500:8500"
            - "8600:8600/udp"
        volumes:
            - ./consul/config:/consul/config:ro
            - ./consul/data:/consul/data
        command: >
            consul agent -server -bootstrap-expect=1
            -ui -client=0.0.0.0
            -config-dir=/consul/config
            -data-dir=/consul/data
        networks:
            - default
```

- [ ] **Step 2: 啟動 Consul 並確認服務正常**

```bash
docker-compose up -d consul
docker-compose logs consul
```

Expected: log 中出現 `Consul agent running!` 且 `agent: Started DNS server` 字樣

- [ ] **Step 3: 確認 Web UI 可存取**

瀏覽器開啟 `http://localhost:8500/ui`，應看到 Consul Dashboard，Services 頁籤顯示 1 個 consul service。

- [ ] **Step 4: Commit**

```bash
git add docker-compose.yml
git commit -m "feat: add consul KV service to docker-compose"
```

---

### Task 3: 建立初始 KV 資料檔

**Files:**
- Create: `consul/kv-init.json`

- [ ] **Step 1: 建立 `consul/kv-init.json`**

```json
[
  { "key": "APP/ENV",   "value": "local" },
  { "key": "APP/DEBUG", "value": "true" }
]
```

> key 命名規則：`<NAMESPACE>/<SETTING_NAME>`，使用 UPPER_SNAKE_CASE。

- [ ] **Step 2: Commit**

```bash
git add consul/kv-init.json
git commit -m "chore: add consul initial KV data file"
```

---

### Task 4: 建立 KV 匯入腳本

**Files:**
- Create: `consul/init.sh`

- [ ] **Step 1: 建立 `consul/init.sh`**

```bash
#!/bin/bash
set -e

CONSUL_URL="http://localhost:8500"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
KV_FILE="${SCRIPT_DIR}/kv-init.json"

echo "Waiting for Consul to be ready..."
until curl -sf "${CONSUL_URL}/v1/status/leader" > /dev/null 2>&1; do
  sleep 1
done
echo "Consul is ready."

echo "Importing KV from ${KV_FILE}..."
python3 - <<EOF
import json, subprocess, sys

with open("${KV_FILE}") as f:
    entries = json.load(f)

for entry in entries:
    key   = entry["key"]
    value = entry["value"]
    result = subprocess.run(
        ["curl", "-sf", "-X", "PUT", "-d", value, "${CONSUL_URL}/v1/kv/" + key],
        capture_output=True
    )
    if result.returncode == 0:
        print(f"  SET {key}")
    else:
        print(f"  FAILED {key}: {result.stderr.decode()}", file=sys.stderr)
        sys.exit(1)
EOF

echo "KV import complete."
```

- [ ] **Step 2: 設定執行權限**

```bash
chmod +x consul/init.sh
```

- [ ] **Step 3: 執行腳本並確認輸出**

```bash
./consul/init.sh
```

Expected output:
```
Consul is ready.
Importing KV from .../consul/kv-init.json...
  SET APP/ENV
  SET APP/DEBUG
KV import complete.
```

- [ ] **Step 4: 在 Web UI 確認 KV 已寫入**

瀏覽器開啟 `http://localhost:8500/ui/local/kv`，應看到 `APP/` 前綴下的所有 key。

- [ ] **Step 5: 確認腳本冪等（重複執行不報錯）**

```bash
./consul/init.sh
```

Expected: 同 Step 3，無錯誤。

- [ ] **Step 6: Commit**

```bash
git add consul/init.sh
git commit -m "feat: add consul KV init script"
```

---

### Task 5: 驗證持久化與容器重啟

- [ ] **Step 1: 重啟 Consul 容器**

```bash
docker-compose restart consul
```

- [ ] **Step 2: 確認 KV 資料保留**

```bash
curl -s http://localhost:8500/v1/kv/APP/ENV?raw
```

Expected output:
```
local
```

- [ ] **Step 3: 確認容器內 PHP 可讀取 KV**

進入 php-fpm8 容器，用 curl 呼叫 Consul API：

```bash
docker-compose exec php-fpm8 curl -s http://consul:8500/v1/kv/APP/ENV?raw
```

Expected output:
```
local
```

- [ ] **Step 4: Commit**

完成驗證後不需額外 commit（無程式碼變更）。
